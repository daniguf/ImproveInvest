import { localeString } from "./localeStringType";
import { newsfeed } from "./news-feed";
import { project } from "./project-schema";

const schemas = [project, newsfeed, localeString];

export default schemas;
